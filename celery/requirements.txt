# --- Core Framework ---
# Django 5.2 provides Long Term Support (LTS) through 2028
Django
# --- API Layer ---
# Option A: Django Ninja (Modern, Fast, Pydantic-based)
django-ninja

# Option B: Django REST Framework (The Industry Standard)
djangorestframework
# OpenAPI 3 support for DRF
drf-spectacular

# --- Database & Performance ---
# PostgreSQL adapter
psycopg2-binary
# Connection pooling and high-perf WSGI server
gunicorn
# For async support in Django (Uvicorn)
uvicorn[standard]

# --- Security & Environment ---
# For handling .env files
python-dotenv
# Security headers for production
django-csp
# CORS headers for API access from other domains
django-cors-headers

# --- Utilities ---
# Image processing
Pillow
# Advanced data validation
pydantic

# --- Asynchronous Tasks & Scheduling (Celery + Redis) ---
# The main task queue framework
celery
# Python client for the Redis broker
redis
# Stores task results in the Django Database (requires 'django_celery_results' in INSTALLED_APPS)
django-celery-results
# Database-backed periodic tasks (requires 'django_celery_beat' in INSTALLED_APPS)
django-celery-beat

python-dotenv
requests
polars

# --- Automatización y Scrapping ---
# Selenium 4.18+ es ideal para Python 3.11
playwright
playwright_stealth