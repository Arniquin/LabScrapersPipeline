# LabScrapersPipeline
Fully orchestrated scrapers pipeline with jupyter lab for development

## Project local setup

Create Django project:
docker compose run --rm django django-admin startproject scrapers .
